# 开发说明

智修 · AI 修图台的技术架构与开发指南。部署相关见 `部署说明.md`。

## 一、项目结构

```
aiBiz/
├── retouch_api.py      # 后端：Flask 单文件应用（全部接口 + AI 调用）
├── static/
│   ├── index.html      # 前端：单页应用（上传页 + 工作区 + 修图对话）
│   └── login.html      # 登录页
├── uploads/            # 用户上传的原图（.gitignore，不入库）
├── README.md           # 项目简介
├── 部署说明.md          # 阿里云部署/运维
└── 开发说明.md          # 本文档
```

无构建步骤：前端是原生 HTML/CSS/JS，后端启动即服务。改动 `static/` 下文件**无需重启**（实时读盘，且响应带 no-cache）；改动 `retouch_api.py` **必须重启**。

## 二、技术栈与运行

- Python 3.9+ / Flask / Pillow / requests（HEIC 支持靠 pillow-heif，可选）
- 本地开发：`PORT=8090 python3 retouch_api.py`（环境变量见部署说明）
- 阿里云：systemd 服务 `ai-biz`，`127.0.0.1:8088`，nginx 反代 `ai.xiabebe.cn`
- 配置：`~/image_analyzer_config.json`（**每次请求实时读取**，改配置不用重启）：

| 配置项 | 用途 |
|--------|------|
| `api_key` / `api_base` / `model` | MiniMax 视觉理解（意图规划、图片描述）+ image-01 重绘兜底 |
| `ty_api_key` | 百炼 DashScope（图像编辑主力） |
| `ty_model` | 图像编辑模型（默认 `qwen-image-edit-plus`，当前用 `qwen-image-3.0-pro`） |
| `ty_api_key_backup` / `ty_api_base_backup` | 备用编辑通道（按量 key）：主通道失败自动重试一次 |
| `ty_model_backup` | 备用通道模型（可选，默认 `qwen-image-edit-plus`，按量端点上比 3.0-pro 快一个量级） |
| `wx_api_key` | 万相独立 key（可选，缺省用 `ty_api_key`） |
| `ty_engine` | `wanx`=万相旧接口（默认 `qwen`=`ty_model` 指定模型） |

## 三、整体架构

```
用户 → 登录页(密码) → 上传图片 ──────────────────────────┐
                                                        ↓
修图对话（整图描述/框选区域/参考图） → POST /api/retouch
                                                        ↓
              MiniMax 视觉模型（理解画面，规划编辑指令：位置/比例/光影）
                                                        ↓
        ┌─────────────────┬──────────────────┬──────────────────┐
        ↓ 整图修改         ↓ 区域修改          ↓ 参考图/文档模式    ↓ 无 ty_key
   qwen 或 万相        qwen 裁剪贴回        只用 qwen           MiniMax
   description_edit  或 万相 mask 重绘    （多图/像素级精确）    image-01 重绘
        └─────────────────┴──────────────────┴──────────────────┘
                                                        ↓
                              预览图（~/.retouch_preview，不动原图）
```

关键设计决策：

- **只出预览，不动原图**：每次修改结果存为 `rt_<uuid>.jpg/.png`，前端用 token 链
  （`base_token`）实现连续修改、向前/向后步骤导航
- **理解层与执行层分离**：MiniMax 负责"看懂图、把用户口语转成精确编辑指令"
  （含比例/位置/光影约束），编辑模型只负责执行。理解层失败时降级为用户原话直发
- **双编辑引擎**：qwen-image-edit-plus（多图参考、同步调用）与万相 wanx2.1-imageedit
  （异步任务、mask 局部重绘）。参考图和文档模式**强制走 qwen**（万相不支持多图，
  文档模式要求框外像素零改动）

## 四、后端接口（retouch_api.py）

| 接口 | 说明 |
|------|------|
| `GET /login` `POST /api/login` `/api/logout` | 密码登录（session，30 天） |
| `POST /api/upload` | 上传 → 统一转 RGB JPEG（长边≤3072）→ `{token,w,h}` |
| `GET /api/image/<token>` | 上传原图 |
| `POST /api/describe_image` | MiniMax 整图描述（上传后自动填输入框） |
| `POST /api/describe_region` | MiniMax 区域描述（框选后自动填该块输入框） |
| `POST /api/retouch` | 修图主接口（见下） |
| `GET /api/preview/<token>` | 结果预览（.jpg 优先，文档/批量为 .png） |
| `GET /api/download/<token>` | 下载结果/原图 |

### /api/retouch 参数与分支

```json
{
  "token": "上传图 token",
  "base_token": "上一次结果 token（连续修改）",
  "instruction": "整图/总体修改描述",
  "history": ["最近6条修改要求"],
  "rect": {"x","y","w","h"},          // 单区域（文档模式用）
  "doc_mode": true,                    // 文档模式：只改框内，框外像素零改动
  "ref_image": "data:image/...",       // 单参考图
  "edits": [{"rect","instruction","ref_image"}],  // 批量区域（每块可挂专用参考图）
  "ref_images": ["data:image/..."]     // 全局参考图（最多2张）
}
```

分支优先级（有 `ty_api_key` 时）：

1. **`edits` 批量**：逐块处理 → 有 `instruction` 再追加总体整图修改。每块流程：
   MiniMax 看整张图规划（框只是位置提示，编辑范围扩 1.6 倍，按比例添加不受框限定）
   → 区域编辑 → PNG 输出
2. **`doc_mode`**：`_ty_doc_edit(doc_precise=True)` 裁剪→qwen 编辑→羽化贴回，PNG
3. **单轮整图**：`_ai_retouch_plan`（MiniMax）→ qwen / 万相整图编辑 → JPEG
4. 无 `ty_api_key` 有 MiniMax key：image-01 重绘兜底

### 核心函数

| 函数 | 职责 |
|------|------|
| `_mm_vision_text/_mm_vision_json` | MiniMax 视觉调用（带一次重试、去 think 段） |
| `_ai_retouch_plan` | 整图修图意图规划 `{reply, edit, prompt}` |
| `_ty_image_edit` | qwen-image-edit-plus，base64 传图（≤2048），参考图在前底图在后，结果临时 URL 立即下载 |
| `_wx_image_edit` | 万相异步任务（创建→轮询≤135s），输入宽高限制 [512,1440] 自动缩放，输出尺寸取整后缩回原尺寸 |
| `_use_wanx` | 引擎路由：`ty_engine=wanx` 且无参考图 |
| `_ty_doc_edit` | 区域编辑：万相走 mask 重绘；qwen 走裁剪→编辑→3px 羽化贴回（doc_precise 保证框外零改动） |
| `_parse_rect` / `_region_desc` | 归一化矩形校验 / 转中文位置描述 |

## 五、前端（static/index.html）

无框架单页，三个状态：上传首页（hero）→ 工作区（workspace）→ 灯箱（lightbox）。

### 关键状态变量

```js
curToken              // 上传原图 token
rtStack=[{token,text}]// 已应用修改栈（画布显示栈顶，空=原图）
rtFwd=[]              // 重做栈（点前一步时压入；新修改清空）
rtRegions=[{x,y,w,h,desc,ref,analyzing}]  // 框选块（归一化坐标）
refImages=[]          // 全局参考图 dataURI（≤2）
rectMode/docMode/busy // 框选模式 / 文档模式 / 请求中
```

### 交互机制

- **框选**：Pointer Events（兼容触屏），`touch-action:none` 防滚动冲突；
  连续拖多块，块位置存归一化坐标，渲染时按图片当前显示尺寸换算（resize 重绘）
- **自动理解**：上传后 `analyzeWhole()` 填主输入框；框选后 `analyzeRegion()` 填该块输入框
  （模板尾「帮我：/把它改成：」未补内容时提交会被剥离，不误当指令）
- **提交**：有框选块 → 批量（区域 edits + 主输入框作为总体描述）；无 → 单轮整图
- **步骤导航**：向后/向前/原图只动 rtStack/rtFwd 指针，预览图都还在磁盘上
- **加载提示**：结果图 onload 前显示「请耐心等待片刻」

改前端注意：JS 全在顶层 `<script>` 里，函数用 `function` 声明（onclick 直接引用）；
改完用 `node -e "new Function(script)"` 验语法。

## 六、开发工作流

```bash
# 1. 本地起服务（配置读 ~/image_analyzer_config.json）
cd /Users/yk/aiBiz && PORT=8091 python3 retouch_api.py

# 2. 登录后测试（密码默认 8888，环境变量 RETOUCH_PASSWORD 可改）
curl -c /tmp/ck -X POST localhost:8091/api/login -H 'Content-Type: application/json' -d '{"password":"8888"}'
curl -b /tmp/ck -F "file=@test.jpg" localhost:8091/api/upload

# 3. 提交（commit 需带身份）→ push → 部署
git -c user.name=yk -c user.email=yk@local commit -m "..."
# 服务器直连 GitHub 不稳，部署走 bundle：
git bundle create /tmp/x.bundle <旧sha>..main && scp /tmp/x.bundle root@47.96.168.224:/tmp/
ssh root@47.96.168.224 'cd /opt/aiBiz && git fetch /tmp/x.bundle main:refs/remotes/origin/main \
  && git merge --ff-only origin/main && systemctl restart ai-biz'
```

## 七、测试要点

- 改 `retouch_api.py`：`python -c "import ast;ast.parse(...)"` 过语法
- AI 链路用真实小图（纯色块合成图即可）跑：上传 → 区域修改 → 验证区域外采样像素不变；
  文档模式必须框外零差异
- 万相引擎注意两个坑：输入宽高需在 [512,1440]（代码已自动缩放）；
  输出尺寸内部取整（代码已缩回原尺寸）
- 百炼 key 验证：401 = key 无效（注意北京/国际站端点之别）
- 清理沙盒：杀进程、删 `uploads/*.jpg`、`~/.retouch_preview`

## 八、常见扩展方向

- **新编辑引擎**：仿 `_wx_image_edit` 加封装，在 `_use_wanx` 处加路由条件
- **新区域交互**：只动前端 rtRegions 与 `/api/retouch` 的 edits 协议
- **结果落库/历史**：目前预览图是临时文件（重启不清理），做持久历史需要加存储层
