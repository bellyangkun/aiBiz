# 部署说明（阿里云）

服务器：阿里云 Ubuntu 24.04（root），与其他服务共用 Python venv `/opt/pyvenv`（Python 3.12）。

## 一、首次部署

```bash
# 1. 拉代码（服务器直连 GitHub 不稳定时用 git bundle + scp 方式，见第四节）
cd /opt && git clone git@github.com:bellyangkun/aiBiz.git

# 2. 装依赖（共用 venv，已装过可跳过）
/opt/pyvenv/bin/pip install flask pillow requests pillow-heif

# 3. 确认 AI 配置已存在：/root/image_analyzer_config.json
#    需要 api_key / api_base / model（MiniMax）和 ty_api_key（通义）

# 4. systemd 服务 /etc/systemd/system/ai-biz.service
```

```ini
[Unit]
Description=aiBiz AI retouch service
After=network.target

[Service]
WorkingDirectory=/opt/aiBiz
Environment=PORT=8088
Environment=BIND=127.0.0.1
Environment=HOME=/root
Environment=PYTHONUNBUFFERED=1
ExecStart=/opt/pyvenv/bin/python /opt/aiBiz/retouch_api.py
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
```

```bash
# 5. 启动
mkdir -p /opt/aiBiz/uploads
systemctl daemon-reload
systemctl enable --now ai-biz
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8088/   # 期望 200
```

数据位置：上传图 `/opt/aiBiz/uploads/`，修图预览 `/root/.retouch_preview/`，均不入库。

## 二、域名（已配置：https://ai.xiabebe.cn）

站点配置在 `/etc/nginx/conf.d/ms-domains.conf` 末尾，HTTPS 证书由 certbot 管理。
**登录在应用层实现**（`/login` 页面，只要密码，session 30 天），nginx 不加 auth_basic：

```nginx
server {
    server_name ai.xiabebe.cn;
    client_max_body_size 35M;   # 上传限制要大于 MAX_UPLOAD_MB
    location / {
        proxy_pass http://127.0.0.1:8088;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;   # AI 修图耗时较长
    }
    listen 443 ssl; # managed by Certbot
    ssl_certificate /etc/letsencrypt/live/ai.xiabebe.cn/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/ai.xiabebe.cn/privkey.pem;
}
```

启用新域名的流程：`nginx -t && systemctl reload nginx` →
`certbot --nginx -d <域名> --non-interactive --agree-tos --redirect --keep-until-expiring`。

登录密码默认 `8888`，通过环境变量 `RETOUCH_PASSWORD` 修改（写在 systemd 单元里）。

## 三、日常更新

服务器直连 GitHub 不稳定（ssh/443 间歇失败），推荐 bundle 方式：

```bash
# 本机
git -C /Users/yk/aiBiz bundle create /tmp/aibiz.bundle <旧sha>..main
scp /tmp/aibiz.bundle root@47.96.168.224:/tmp/

# 服务器
cd /opt/aiBiz
git fetch /tmp/aibiz.bundle main:refs/remotes/origin/main
git merge --ff-only origin/main
systemctl restart ai-biz
curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8088/
```

## 四、运维命令

```bash
systemctl status ai-biz          # 状态
journalctl -u ai-biz -f          # 实时日志
systemctl restart ai-biz         # 重启（改代码后必须）
```

## 五、环境变量

| 变量 | 默认 | 说明 |
|------|------|------|
| `PORT` | 8088（本服务）/ 8090（代码默认） | 监听端口 |
| `BIND` | 127.0.0.1 | 监听地址，经 nginx 反代不要改 0.0.0.0 |
| `UPLOAD_DIR` | `<代码目录>/uploads` | 上传图存储 |
| `PREVIEW_DIR` | `~/.retouch_preview` | 修图预览（HOME=/root → /root/.retouch_preview） |
| `MAX_UPLOAD_MB` | 30 | 上传大小上限，nginx 的 client_max_body_size 要与之匹配 |
