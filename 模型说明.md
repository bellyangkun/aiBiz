# 模型能力说明：谁能看图，谁能改图

本文解释 aiBiz 修图服务背后的模型分工，以及"多模态"这个容易误解的概念。

## 一、"多模态"的两个方向

| 类型 | 数据流向 | 能力 | 代表模型 |
|------|----------|------|----------|
| 多模态**理解**模型 | 图片+文字 进 → **文字** 出 | 看图说话、视觉问答、内容分析 | MiniMax-M3、qwen-vl 系列、qwen3.6/3.7/3.8（视觉版）、GLM-5.x |
| 多模态**生成**模型 | 图片+文字 进 → **图片** 出 | 文生图、图生图、图像编辑、多图参考 | wan2.7-image(-pro)、qwen-image-2.0/3.0-pro、qwen-image-edit-plus |

一个模型标着"多模态"，**默认只是能"看懂"图，不代表能"生成/修改"图**。
MiniMax-M3 就是典型的理解型：它能准确描述画面、判断透视比例、规划修改方案，
但输出永远只有文字，动不了图上一个像素。

## 二、MiniMax 的图像能力边界

MiniMax 全线只有两个图像生成模型：`image-01`、`image-01-live`。
它们文档里写的"图生图"**不是编辑**，而是 `subject_reference`（人物主体参考）：

- 传一张含人物的图 → 提取人物**特征** → 按 prompt **重新生成**一张新图
- 没有 mask、没有区域编辑、没有"其余部分保持原样"的概念
- 非人物的画面元素基本保不住，输出尺寸也只能从固定比例里选

所以 MiniMax 体系做不了"把天空变蓝、其余不动"这种编辑。它在我们架构里的位置：

- **理解层（主力）**：M3 看图 → 生成精确编辑指令（位置/比例/光影约束）
- **重绘兜底（应急）**：完全没配百炼 key 时，用 image-01 的 subject_reference
  整图重绘——效果远不如正经编辑，只是保证功能不瘫

## 三、百炼（阿里）的编辑模型

真正能做图像编辑的是百炼的生成型模型，共同特点：
**图片+文字指令进，改好的图片出**，其余内容尽量保持。

| 模型 | 特点 | 输出尺寸 |
|------|------|----------|
| qwen-image-3.0-pro | 复杂排版/密集信息强（改文档、海报首选） | ~2352×1760 |
| wan2.7-image-pro | 编辑幅度果断，多图参考稳定 | ~2364×1773 |
| qwen-image-2.0(-pro) | 输出接近原图尺寸，文件轻 | ~1184×896 |
| qwen-image-edit-plus | 老牌编辑专用模型（不在新套餐里） | 接近原图 |

通用规则：输入图片宽高需 384~3072；参考图在前、底图在后（输出比例以底图为准）；
结果图 URL 限时有效需立即下载。

## 四、aiBiz 的双层流水线

```
用户口语描述（+框选/参考图）
        ↓
【理解层】MiniMax-M3（视觉理解）
  看整张图 → 把口语转成精确编辑指令
  （明确位置、按比例决定大小、光影色调一致、其余保持原样）
        ↓
【编辑层】qwen-image-3.0-pro（图像生成/编辑）
  原图 + 编辑指令 → 输出修改后的图
        ↓
预览（不动原图，可继续改/向前向后/下载）
```

设计要点：

- **理解层可随便换**：任何会看图的模型都行（MiniMax-M3 / qwen3.6-flash / GLM），
  理解失败时降级为用户原话直发编辑层
- **编辑层选择面窄**：必须是真正的图像生成模型，且要支持"带图编辑"——
  目前只有百炼这几个模型可用，通过 `ty_model` 一个配置项切换
- **计费通道可切换**：编辑层走 Token Plan 套餐（`ty_api_base` 指向
  token-plan 域名）或按量付费，主通道失败自动用备用 key 兜底

## 五、给功能选模型的速查

| 需求 | 用的模型类型 | aiBiz 现状 |
|------|--------------|-----------|
| 看图填描述、规划修改 | 视觉理解模型 | MiniMax-M3 |
| 整图修改（改色/风格/去物） | 生成模型·带图编辑 | qwen-image-3.0-pro |
| 区域精确修改 | 生成模型 + 裁剪贴回 | 同上（代码控制区域） |
| 参考图加人/物体 | 生成模型·多图参考 | 同上（参考图+底图） |
| 文档改字（框外零改动） | 生成模型 + 裁剪贴回 | 同上（doc_precise 路径） |
| 视频生成 | 视频模型（异步任务） | 未接入（套餐有 happyhorse-1.1） |
